<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>JavaScript</title>

</head>
<body>
    <h1>JavaScript</h1>

   
    <main>
        <h4>Eigenschaften</h4>
        <ul>
            <li>Skriptsprache (aus Performanzgründen aber compiliert - z.B. V8 in Chrome, SpiderMonkey in Firefox)</li>
            <li>dynamische Typisierung</li>
            <li>keine unterschiedlichen Referenztypen</li>
            <li>Vererbung durch <code>prototype</code></li>
            <li>Objekteigenschaften und -funktionen können einfach dem Objekt hinzugefügt werden</li>
        </ul>
        <h4>Nützliche Links</h4>
        <ul>
            <li><a href="https://www.ecma-international.org/publications-and-standards/standards/ecma-262/">ECMA-262</a></li>
            <li><a href="https://dom.spec.whatwg.org/">Document Object Model (DOM)</a></li>
            <li><a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide">JavaScript Guide</a></li>
            <li><a href="https://www.w3schools.com/js/default.asp">JavaScript Tutorial</a></li>
            <li><a href="https://learnjavascript.online/">Learn JavaScript</a></li>
            <li><a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference">JavaScript Reference</a></li>
        </ul>
        <h4>Ergebnisliste</h4>
        <ul id="ulresult" style="color: green;"> 
            <li id="liresult1"></li>
        </ul>
    <input type="text" id="input" oninput="inputFunc()" onfocus="changeColor()" value="Hallo" placeholder="Name"/>
     <button type="button" onclick="helloFIW()"> Klick Mich!</button>

   
    </main>
     <script scr="">

        function inputFunc()
    {
            //let inputElement = documentgetElementById('input')
            let inputElement = document.querySelector('#input')
            console.log(inputElement.value)
            let liElem= document.querySelector('#liresult1')
            liElem.textContent = inputelement.value
           // liElem.style.color = "red"
           liElem.innerHTML =`Eingabe <ul><li style='color: red;'>${inputElement.value}</li></ul>`
        }

        function changeColor(){
            let inputElement = document.querySelector('#input')
            inputElement.style.backgroundColor = "yellow"
        }
        // JavaScript

        let x = 5;
        x = "FIW";

        const y = 6; // kann keine neue konstante annehmen
      //  y = "FIW"; 

        var a = 6;
      //  a = 6;
        function HelloFIW(a, b){
            return a + b;
        }
        console.log("HelloFIW : " + HelloFIW(3,4));
        console.log("HelloFIW : " + HelloFIW(5,6));
        console.log("HelloFIW : " + HelloFIW(4,4));

        console.log("x= "  + x);
        console.log('x= ' + x);
       // console.log(‘x=  +  ${x}‘);

        const test2 = test1;
        console.log("test2 : " + test2(3,4));

       const test3 = (a, b) => a + b;
        console.log("test3 : " + test3(3,4));
    </script>

</body>
</html>